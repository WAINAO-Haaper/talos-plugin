# Changelog

> 本文件记录 TALOS 插件的开发变更历史。
>
> 起始日期：2026-06-23（v0.1.0）— 最近：2026-07-09


- 2026-07-09 TALOS 主控制台启动顺序二次修复：根据用户实机截图与当天修改链路复盘，确认当前空白页不是目录颜色本身、也不是总览模块对齐本身；`talos-console-view` 已写入 Obsidian workspace，但主插件恢复 `ClaudianWorkbenchPlugin` 继承后又把 `await super.onload()` 放回 TALOS 主视图注册之前，屈原/Claudian 初始化一旦卡住，Obsidian 只会恢复一个空白旧页签。现改为先加载 TALOS 设置、注册 `talos-console-view`/旧屈原回滚视图、Ribbon、命令、设置页与全库主题，再异步初始化屈原完整工作台；完整工作台未完成时只影响「打开屈原完整工作台」入口，不再拖垮 TALOS 控制台。诊断报告同步显示工作台“已完成 / 初始化中 / 失败”状态，`quyuan-v2.selftest.mjs` 增加启动顺序断言，防止回归。
- 2026-07-09 屈原 Obsidian 内诊断链路：用户实机反馈完整工作台仍无法打开后，新增运行时诊断闭环。命令面板增加「生成屈原诊断报告」，会写入 `System/reports/talos-quyuan-diagnostics-*.md` 并记录插件版本、`talos-quyuan-view` leaf 状态、人格启动状态、工作台初始化错误、安全设置快照和最近 `window.error/unhandledrejection`。`activateQuyuanV2View()` 现在捕获打开异常并自动落诊断；打开后 1.2s 自检 shell/tabManager，若视图创建但内部未完成挂载也会写报告。`ClaudianView.onOpen()` 增加页面内错误面板，提供“重试”和“生成诊断”，避免完整工作台内部异常只落在开发者控制台里。`npm run typecheck`、`npm run test:quyuan`、生产构建、安装态语法检查和产物比对已通过，并部署回 `.obsidian/plugins/talos/`，`data.json` 未覆盖。
- 2026-07-09 屈原完整工作台加载链路恢复：排查确认“屈原完整工作台无法加载”的直接根因是前一轮静态依赖隔离把 `TalosPlugin` 从 `ClaudianWorkbenchPlugin` 退回普通 `Plugin`，并把 `activateQuyuanV2View()` 硬编码为隔离 Notice，导致 v2 视图注册、Provider 工作台初始化和高级设置融合断链。现恢复主插件继承内嵌 Claudian 工作台基类，但通过 `shouldRegisterWorkbenchRibbon/SettingTab=false` 继续隐藏重复入口；`activateQuyuanV2View()` 改为在 Obsidian 主编辑区打开 `talos-quyuan-view`，并在人格上下文缺失或工作台初始化失败时给出明确 Notice。同步恢复人格上下文写入工作台 `systemPrompt` 的标记块，`src/settings.ts` 的「屈原 · 高级」设置页也回到真实工作台类型。`npm run typecheck` 与 `npm run test:quyuan` 已通过。
- 2026-07-09 屈原语音页高度护栏：确认 TALOS 总览、每日执行等页面均可渲染，问题范围收敛到「屈原」语音模块。根因方向为今天的详情页窄导航/总览重排样式叠加旧 `jarvis` 满高规则，使 `.main/.page-content/.tq-voice` 在父级高度链路不稳定时被压空；同时 ASR 已请求麦克风权限，说明语音模块代码在执行但 UI 不可见。现于 `styles.quyuan-shell.css` 增加屈原页最终布局护栏：为 jarvis 页定义独立 `--tq-view-height`，强制 `.app/.main/.page-content/.tq-voice` 满高可见并隐藏溢出；移除此前用于定位主视图的红色 `TALOS DEBUG` 诊断条，避免遮挡界面。
- 2026-07-09 TALOS 空白页复活诊断：针对 Obsidian 继续复用旧崩溃标签页的问题，`activateTalosView()` 与启动首页逻辑改为对已有 `talos-console-view` 重新 `setViewState()`，并在视图实例未渲染出 shell 时主动重建；`TalosView` 增加启动诊断条、空白检测和时钟计时器清理。若实机仍白屏，诊断条是否出现可直接区分“插件代码未被加载/旧实例未替换”和“页面已进入但渲染被后续逻辑或样式阻断”。
- 2026-07-09 TALOS 启动期静态依赖拆除：二次确认空白页仍存在后，定位到 `main.ts`、`view.ts`、`settings.ts` 仍有 Claudian/屈原完整工作台静态导入；这些模块在插件文件求值阶段失败时，`onload()` 级兜底不会执行。现将主插件改回直接继承 Obsidian `Plugin`，主控制台启动不再继承 Claudian 主类；屈原语音页 `QuyuanVoicePanel` 与「屈原 · 高级」设置页均改为点击后动态加载。屈原完整工作台入口临时隔离为 Notice，先保证 TALOS 控制台、目录颜色、总览统计和审批按钮可打开。类型检查、审批按钮自测、审批执行器自测、许可证审计、生产构建、安装产物比对和 `main.js` 语法检查通过。
- 2026-07-09 TALOS 主视图加载隔离：确认「TALOS 控制台」空白不是屈原页内部挂载问题，而是主插件 `onload()` 先执行 Claudian/provider 初始化；该阶段任一异常都会阻断 TALOS 自身视图注册，旧标签只剩空白。现调整为先加载 TALOS 设置、注册控制台视图、命令、图标和设置页，再隔离执行屈原完整工作台初始化；Claudian 初始化失败只影响完整工作台入口，不再拖垮 TALOS 控制台。`TalosView.onOpen()` 同步增加主视图级错误面板，后续若刷新数据失败会显示具体错误与重试按钮，不再白屏。
- 2026-07-09 屈原模块白屏恢复：针对控制台「屈原」页打不开、主区空白的问题，给 `pageJarvis` 挂载入口增加异常兜底；挂载失败时显示「屈原模块加载失败」恢复面板，提供重试加载和打开完整工作台，不再留下白屏。`voice-panel.ts` 对 Canvas 粒子层和 ASR 自动启动增加局部降级：粒子不可用时进入静态语音模式，语音输入不可用时保留文字输入与工作台入口。`styles.talos.css` 和 `styles.quyuan-shell.css` 同步补齐错误面板与粒子降级视觉。
- 2026-07-09 插件内置 Obsidian 目录颜色：按用户当前 vault 的 Notebook Navigator 根目录彩虹配置（root scope / hue transition / light `#ef4444→#8b5cf6`）固化为 TALOS 插件全局样式层。TALOS 启用并同步全库主题时，默认文件目录与 Notebook Navigator 的根目录文字、图标、展开箭头、悬停底色使用同一套目录颜色；颜色不随 TALOS 七套主题漂移，确保用户安装插件后目录配色与当前超级大脑目录观感一致。本次只交付颜色，不复制 Icon Folder 的图标数据。
- 2026-07-09 总览行动队列双栏对齐：按实机截图反馈，将总览桌面双栏从自然高度改为等高拉伸；左侧行动列拆成「运行态势 + 行动队列」两段，行动队列吃满右侧健康统计墙的剩余高度，队列行等高铺开，审批按钮入口贴底，视觉底线与右侧纵向柱状统计区对齐。1250px 以下恢复自然单列，避免移动端被强制撑高。
- 2026-07-09 全模块变色动效统一：按反馈把新增健康分圆环、纵向柱状图、行动队列纳入既有「全模块选中反馈」体系，同时覆盖总览指挥卡、优先级卡、旧指标卡、小状态卡、业务页 quick/metric/project/talos/module、detail-row、note、focus、gate、signal-pill、approval、daily-item 与模块首屏卡。统一为同一套 hover/focus/selected：强调色染底、边框提亮、轻微位移、统一阴影节奏；健康圆环、柱状图填充和统计墙扫描停止独立呼吸/闪光，避免新增模块与前期搭建模块动效不一致。七主题分别保留原主题语义：Animal Island 按压，小黑白反相，Data Stream 终端绿，Soft Relief 内凹，Geometric Modern 黑框色块。
- 2026-07-09 总览图形统计与左侧去重：按实机反馈移除左侧重复的任务/建议模块，左侧只保留运行态势、第一优先级和一个「行动队列」列表，审批按钮入口继续挂在行动队列下。右侧统计墙改为「健康分统计」主图 + 纵向柱状图：健康分用圆环展示，数据源、刷新时间、断链作为健康分子指标；其余执行、发布、知识、输出、项目、洞察、素材、能力、TALOS 资产和身份上下文改为纵向柱状图，减少纯文字卡片占比。
- 2026-07-09 总览行动/统计双栏：按反馈把总览内容重排为左侧行动列、右侧动态统计墙。左侧集中运行态势、待审批、待办焦点、收件箱、偏好候选、待处理队列、今日焦点和处理建议；右侧新增高密度统计矩阵，覆盖今日执行、发布闭环、系统准备度、数据新鲜度、知识总量、输出资产、项目场景、原创洞察、外部素材、能力中心、TALOS 资产、身份上下文和断链风险。统计墙增加横向扫描与指标卡 glint 动效，桌面最多三列，窄屏降级为两列/单列，并尊重 `prefers-reduced-motion`。
- 2026-07-09 总览 Hero 标题放大与巡航降速：按反馈将总览标题模块继续放大，Hero 基准高度从 224px 提到 252px，标题字号从 58px 提到 64px，`TALOS` 英文字段比例加大到 1.24em、中文字段加到 0.76em。像素人主巡航动画从 7.2s 放慢到 12s，轨道流速同步放慢；巡航容器从 bottom 14px 压到 2px，轨道和像素人脚底更贴近 Hero 底部边框。
- 2026-07-09 总览像素人往返转身：按反馈取消底部像素人的淡出重置，`tl-pixel-patrol` 改为左右往返巡航；到右端短暂停留后 `scaleX(-1)` 转身返回，到左端保持朝向后进入下一轮，不再出现消失闪烁。
- 2026-07-09 总览 Hero 标题比例与底部巡航：按实机反馈删除标题下方「AI 系统架构师 · 个人上下文主权 · C 端布道 × B 端交付」小字；标题 DOM 拆成 `talos-title-latin` 与 `talos-title-cn`，让 `TALOS` 英文放大、中文「系统控制台」缩小并保持基线对齐。像素巡航层从标题右半区移到 Hero 模块底部，轨道覆盖模块底部横向空间。
- 2026-07-09 总览 Hero 动画区放大：按实机反馈移除 Hero 底部「模型会商品化，上下文会差异化」主张卡 DOM，把总览顶部改为标题 + 大巡航动画结构；像素巡航层移到 Hero 右半区，轨道宽度由固定小块升级为 `left/right` 自适应长轨，像素人物按 `--pixel-scale` 放大并扩大往返范围。右上 Logo 模块与内部 TALOS 图标同步缩小，Hero 右列从 126px 收到 86px，避免 Logo 继续抢占动画空间。
- 2026-07-09 总览 Hero 像素巡航台：按用户方案把「TALOS 系统控制台」标题区升级为动态模块；`src/view.ts` 在总览 Hero 内新增只读装饰层 `talos-pixel-patrol`，由右侧 Logo 基地方向生成 TALOS 像素机器人、像素轨道、眼睛眨动和步行动作。`styles.talos.css` 新增标题扫描高亮、像素人往返巡航、轨道流动、七主题配色覆盖与 `prefers-reduced-motion` 静止降级；移动窄屏隐藏巡航层，避免遮挡标题。
- 2026-07-09 总览零缝隙铺满：将总览紧凑布局升级为贴边布局，外层 `.app` padding、侧栏/主栏 gap、`page-content` gap、主判断区、指标矩阵、二级状态卡和底部工作区 gap 均清零；总览卡片 hover/常态阴影关闭，避免阴影和深色背景形成视觉黑缝。Hero、主判断、指标卡、二级状态卡内边距同步压缩，保留边框作为模块分隔线。类型检查、审批按钮自测、模型执行自测、许可证审计、生产构建、部署和产物比对通过。
- 2026-07-08 总览模块紧凑铺满：新增总览页专用紧凑布局层，压缩外层 `.app` padding、侧栏/主栏 gap、`page-content` gap、Hero 高度、主判断/指标矩阵/二级状态/底部工作区之间的间距，并降低 Geometric Modern 总览卡片阴影占位；桌面总览导航列从 296px 收到 286px，释放主内容宽度。目标是减少模块之间的黑色缝隙，让总览模块更贴近并铺满页面。类型检查、审批按钮自测、模型执行自测、许可证审计、生产构建、部署和产物比对通过。
- 2026-07-08 详情页隐藏导航锁定：将非总览页的 72px 隐藏导航轨从普通文档流改为 `position: sticky`，并给 `.app` 增加 `align-items:start`，避免 grid 拉伸导致 sticky 失效；保留悬停/聚焦展开到 296px（窄屏 260px）的行为，主内容滚动时导航轨不再跟随页面上下移动。屈原页单独恢复 `align-items:stretch`，避免影响满高工作台。类型检查、审批按钮自测、模型执行自测、许可证审计、生产构建、部署和产物比对通过。
- 2026-07-08 总览底层模块悬停变色修复：补齐 `overview-secondary-item` 与 `overview-workgrid` 面板的 hover 背景变化，解决总览下方“小状态卡 / 今日焦点 / 处理建议”两层模块在 Geometric Modern 白纸主题下只动边框和阴影、底色仍保持白色的问题；Geometric Modern 总览页额外加入强覆盖，让底层小状态卡变黄、底部面板按自身强调色染底。类型检查、审批按钮自测、模型执行自测、许可证审计、生产构建、部署和产物比对通过。
- 2026-07-08 审批按钮选中态可读性修复：待审批入口按钮改为 CSS 变量驱动，并在全库主题同步层之后补高优先级覆盖，锁定 `批准 / 拒绝 / 批准+模型` 在普通、悬停、选中、按下、禁用状态下的背景色、边框色、文字色和图标色，避免 `body[data-talos-vault-theme] button !important` 与 `theme-system-classic` 选中态把按钮刷成浅底白字。类型检查、审批按钮自测、模型执行自测、许可证审计、生产构建、部署和产物比对通过。
- 2026-07-08 总览模块缩小重排与颜色统一：按用户反馈“不要横向拉伸，可以缩小模块重新排列”，将方案 A 的横向指挥摘要继续收束为 `overview-reflow`：左侧 `overview-command` 只放运行判断、第一优先级和主动作，右侧 `overview-command-metrics` 改为 2×2 指标矩阵，并把数据新鲜度提升为第四张指标卡；二级状态改为四张小卡，底部拆出今日焦点与处理建议两个下钻面板。随后统一总览卡片底色，右侧指标卡和二级状态卡改回与主模块一致的纸面背景，只保留红色细节和进度条提示；本轮同步强化总览上部指挥卡、优先级卡和指标卡的悬停变色、边框提亮、光晕和轻微上浮反馈。类型检查、许可证审计、生产构建、部署、产物比对和 `data.json` 哈希检查通过。
- 2026-07-08 全业务模块首屏套用单模块优化方案：将总览确认的“缩小模块、重新排列、统一颜色”推广到 `moduleHero` 共用组件，覆盖每日执行、输出、TALOS 产品、收件箱、系统健康、项目场景、知识枢纽、身份上下文、能力中心、全库视图 10 个业务页。模块首屏改为左侧纸面主说明、右侧 2×2 纸面统计矩阵、下方统一动作区；随后按方案 A 给左侧标题卡恢复模块身份色，通过浅色底、6px 色条、图标和英文小标题突出当前模块，统计卡和动作区仍保持统一纸面与红色细节。最新强化顶部模块统一 hover：标题卡按模块身份色大幅提亮，统计卡按红色操作色明显染底，按钮保持统一动作色，三者均轻微上浮且尊重 `prefers-reduced-motion`。类型检查、许可证审计、生产构建、部署、产物比对和 `data.json` 哈希检查通过。
- 2026-07-08 总览方案 A 指挥摘要落地：按用户选择的「指挥摘要型」优化总览页，移除原先运行结论、三指标、异常队列、处理建议、今日焦点、数据新鲜度五个同级大面板的抢眼结构，改为一个 `overview-command` 指挥摘要模块：运行态势、一张第一优先级卡、三枚结果指标同屏；其余待处理、焦点和数据新鲜度进入二级状态面板。总览 Hero 同步压矮为品牌识别层，避免占据首屏主注意力。类型检查、许可证审计、生产构建和部署通过，`data.json` 哈希未变。
- 2026-07-08 业务模块首屏统一优化：为每日执行、输出作战室、TALOS 产品、收件箱、系统健康、项目场景、知识枢纽、身份上下文、能力中心、全库视图 10 个业务页新增共用 `moduleHero` 首屏组件，按页面注入真实统计、文件入口和复制命令动作；总览继续保留主 Hero，屈原页保持独立语音工作台。样式新增统一模块皮肤、响应式单列与 `prefers-reduced-motion` 回退，指标长文本允许换行避免裁切；动作按钮补齐全库主题同步层的高优先级覆盖，避免实机被 Obsidian 普通按钮样式重绘。类型检查、许可证审计与生产构建通过。
- 2026-07-06 屈原语音低延迟与误打断修复：修正模型文字结束即 `setBusy(false)`、但云端 TTS 仍在排队/播放的生命周期错位，避免屈原把自己的朗读重新识别为用户输入。模型生成、TTS 排队、真实播放改为三态联合保护；自动声控打断只在真实播放期开放，思考和网络排队阶段保留手动“立即打断”。VAD 打断门槛 `.06/320ms` 收紧为 `.09/600ms`，新增 600ms 播放保护窗；自动断句静音窗 800→550ms。语音 runtime 使用独立设置快照并默认 Haiku + Low、面板挂载/唤醒时预热，不改变文字工作台 Opus High；TTS 无标点首段 80→28 字。设置页新增语音模型/思考档。Claudian 固定快照零改动；类型检查、定向 lint、语音自测、许可证审计与生产构建通过，产物已部署且 `data.json` 哈希未变，待 Obsidian 实机回验。
- 2026-07-05 非总览页面统一悬浮窄导航：将屈原页验证后的 72px 图标轨推广到每日执行、收件箱、输出、项目、知识、身份、TALOS、健康、能力、全库等全部 11 个详情页；总览继续保留 296px 完整导航。详情页悬停/键盘聚焦覆盖展开至 296px，≤1080px 展开 260px，≤760px 静止轨道 54px；时钟、快捷入口和审批卡不再占详情页侧栏。主内容同步使用释放空间：1600px 为 72+1528、1024px 为 72+952、390px 为 54+336，展开时主区宽度不变。七主题均验证 12 个图标完整、无横向溢出；类型检查、定向 lint、生产构建与自测 62/62 通过。
- 2026-07-05 屈原窄轨裁切修复：实机截图显示 Geometric Modern 的完整卡片外框、14px 内边距和主题投影仍被带入 72px 收起态，导致右边框裁断、顶部标题装饰残片外露。现仅在收起态移除导航容器外框/内边距/背景/投影及伪元素，12 个 50px 图标按钮居中完整显示；展开后自动恢复 296px 主题卡片、四组分类、12 个标题和数字。1600×1000 回验 12/12 按钮均位于 72px 轨道内（x=10.5–60.5px），页面无横向溢出；类型检查、定向 lint、生产构建与自测 62/62 通过。
- 2026-07-05 屈原页导航改为悬浮窄轨：根据完整导航实机回验，将屈原页默认侧栏收为 72px，只显示 12 个可识别图标；鼠标移入或键盘聚焦时覆盖式展开到 296px（≤1080px 为 260px），完整显示四组分类、页面名和实时数字。展开不改变主舞台宽度，主区在 1600px 视口始终保持 1528px，无横向溢出；导航补齐 button 语义、Tab 聚焦与 Enter/Space 激活。现有七主题材质、屈原舞台、底栏和右侧交互面板不变。类型检查、定向 lint、生产构建与自测 62/62 通过。
- 2026-07-05 屈原页完整导航恢复：修复旧版 68px 图标轨道规则继续覆盖新版四域导航，导致页面名被压缩为首字、状态徽标单独悬空的问题。屈原页桌面侧栏恢复为 296px，中等窗口为 260px；导航标题、四组分类、12 个页面名、30px 图标和实时数字全部显示，主舞台与右侧交互面板在剩余空间内自适应。Geometric Modern 激活项“屈原”另加深墨色，避免浅色选中底上的渐变字失去对比。1600×1000 回验侧栏 296px、主区 1304px、12/12 标签可见且页面无横向溢出；类型检查、定向 lint、生产构建与自测 62/62 通过。
- 2026-07-04 浅色导航对比度与七主题 Hero 统一：修复浅色主题非激活导航标题继续继承 Aurora 白色渐变、在浅底上几乎不可见的问题；保留四域色彩语义，以主题墨色 80% 混合分组强调色，最弱的 Animal Island 琥珀组对比度由 4.16:1 提升至 4.74:1。总览「TALOS 系统控制台」统一为同一内容骨架：260px 基准高度、57.6px 桌面标题、112×100px Logo 模块和单行主张卡；宇宙主题取消独立全幅场景，改用同骨架叠加轨道/星云背景。七主题继续保留各自材质、色彩、边框、阴影与动效；1440×1024 与 390×844 均无横向溢出，类型检查、定向 lint、自测 62/62 和生产构建通过。
- 2026-07-04 导航尺寸与比例修正：据 Geometric Modern 实机截图修复分组导航整体偏小、分类标题难辨的问题。总览桌面导航列统一为 296px，分类标题 9→12px，页面名统一为 15px，导航卡最小高度 38→48px，图标 24→30px，状态徽标同步放大；390px 保留横向轨道并采用 14px 页面名、26px 图标。只调整几何比例和字号，四域分组、页面顺序、实时数字与七套主题材质不变。七主题 1440×1024 无横向溢出，390×844 总览无横向溢出；类型检查、定向 lint、自测 62/62 与生产构建通过。
- 2026-07-04 导航真实结构重构：按库内实际职责把 12 个页面归为「现在 / 流转 / 资产 / 系统」四组，导航状态数字实时读取焦点、异常、收件箱、输出、项目、知识、身份、TALOS、健康、能力与全库数据。新增「身份上下文」页面，原生呈现 Identity、灵魂与 working-memory 的真实入口；`TALOS 漏斗` 更名为更准确的 `TALOS 产品`。日志、归档、模板、自动化、配置、部署模板与附件不再竞争一级入口，继续在全库/系统聚合页中呈现。七主题、1440×1024、390×844、鼠标与键盘选择回验通过；自测 62/62、类型检查、定向 lint 与生产构建通过。
- 2026-07-04 总览主控台恢复：按用户实机截图纠正上一轮范围判断，保留总览顶部原有「TALOS 系统控制台」Hero；方案 3 的运行态势与方案 2 的异常工作区继续位于其下。只移除总览对 `.main > .hero` 的隐藏选择器，时钟/快捷入口/审批侧栏精简仍保留。七主题桌面回验均显示 Hero，390px 无横向溢出；类型检查、定向 lint 与生产构建通过。
- 2026-07-04 总览行动化改版：按方案 3 建立「运行结论 / 三个结果指标 / 今日焦点 / 数据新鲜度」骨架，按方案 2 增加可选择的异常队列与处理建议工作区；所有数字继续来自任务流、发布作战室、健康记录、审批池、收件箱和偏好候选池，不引入演示数据或无行动价值的图表。仅新增布局和信息层级，七套主题令牌、组件材质及其他页面均不改；680px 以下导航压缩为横向轨道。1440×1024、390×844、七主题、鼠标/键盘交互和同屏设计对照通过，证据见 `prototype/overview-operations-*` 与 `design-qa.md`。
- 2026-07-03 屈原完整 Agent 工作台主题同步修复：根因是 `talos-quyuan-shell` 固定浅色宣纸背景/墨色/`color-scheme: light`，同时全库同步层又以当前主题的 `!important` 重绘按钮与链接，形成“白底 + 主题色浅字 + 深色按钮”的混合态。现移除工作台对 Obsidian 基础变量的覆盖：开启全库同步时统一消费七套 `--tv-*` 令牌，关闭时自然继承 Obsidian 明暗主题；背景、文字、边框、强调色、圆角与 `color-scheme` 同源即时切换。新增自测防回归，数据流深色、几何现代浅色与 Obsidian 深色继承三态浏览器回验通过；自测 62/62、类型检查、许可证审计与生产构建通过，产物已部署且 `data.json` 哈希未变。
- 2026-07-03 屈原宣纸晨雾背景与顶部品牌栏：按六案中的第 3 案，将 Geometric Modern 下的屈原语音页改为 `#f1ebdd` 暖宣纸底、深墨文字、浅纸面板和矿物色粒子；保留已确认的黑色 Uiverse 胶囊按钮。顶部重组为品牌/动作两组，原“屈”字徽章替换为 `02-品牌资产/` 的 TALOS Modular T-Shield 定稿图标；72px 桌面栏和 390px 窄屏均无溢出。主文字、次文字、浅文字对比度分别为 13.20:1、5.75:1、4.56:1。选定稿、实现截图、全屏/顶部组合对照和 `design-qa.md` 均已落盘，QA 最终 `passed`；typecheck、定向 lint、自测 62/62、生产 build 通过。`main.js`、`styles.css`、Logo、字体与许可证已部署，`data.json` 未覆盖。
- 2026-07-03 屈原四态粒子动效与舞台重排：状态流程放大并移至左上，粒子核心从 49%/46% 校正为舞台 50%/50%，状态标题、回答流与实时文字移至右下。粒子数 720→840；聆听、识别、思考、回答分别使用音量呼吸、扫描收束、双向涡旋、外扩波阵，切态增加 720ms 能量过渡。浅色宣纸改用 source-over 实体混合，避免 lighter 把矿物色洗白；深色主题仍用 lighter。回答态实机基线、实现截图、全屏及舞台聚焦对照已落盘，`design-qa.md` 最终 `passed`；typecheck、定向 lint、自测 62/62、生产 build 通过。
- 2026-07-03 屈原粒子动态流色增强：在四态运动语义上叠加逐粒子相位流色。聆听为矿蓝冷呼吸，识别为蓝绿扫描，思考为蓝紫涡旋，回答为朱砂/矿蓝/苔绿交替波阵；每 19 颗粒子抽取 1 颗绘制低透明动态光晕，脉冲环与轨道同步进行状态色渐变。Geometric Modern 的矿物色加深但不改宣纸底与文字层级。回答态首次回验发现暖色过量后降低状态色覆盖并引入冷色闪带；最终同尺寸全屏与舞台聚焦对照通过，typecheck、定向 lint、自测 62/62、生产 build 通过并部署，`data.json` 未覆盖。
- 2026-07-03 屈原语音界面统一按钮与艺术标题：将顶栏、语音底栏、右侧标签、快捷提示、列表入口、发送与危险确认收拢到 `tq-btn` 语义体系；紧凑操作按钮采用 Uiverse.io `gharsh11032000` 的深色胶囊、底部上涌填色与 200ms 摇动。首次独立预览漏掉了真实 Obsidian 的 `body[data-talos-vault-theme] button !important` 覆盖层，导致实机仍为米白按钮；现已增加屈原范围内的高优先级覆盖，并把 QA 页接入 `geometric-modern` 全库主题同步层。二次实机截图补齐右侧“会话 / 上下文 / 能力”页签：三项统一黑色胶囊和上涌填色，选中项保留加深后的语义色，浅色文字/图标对比度为 4.88:1；页签不旋转，避免相邻控件碰撞。所有会上涌的直文本按钮改为显式文字层，防止填色遮字。主状态标题使用本地 Ma Shan Zheng 免费毛笔体。真实主题桌面与 390×844 均无横向溢出；typecheck、定向 lint、自测 62/62、生产 build 通过。字体、许可证、`main.js`、`styles.css` 已部署到 `.obsidian/plugins/talos/`，`data.json` 哈希部署前后不变。
- 2026-07-01 总览 Hero 作用域修正：将顶部「TALOS 系统控制台」模块从全页常驻改为仅在「总览」页显示；每日执行舱等全部子页保留左侧导航，但主区域直接展示本页信息。规则统一作用于七套主题，屈原原有满高工作区逻辑保持不变。
- 2026-07-01 商用许可边界加固：纠正模板遗留的 `0-BSD` 与 Dynalist 版权错误。TALOS 自有代码改为 `UNLICENSED` + `private:true`，根 `LICENSE` 明确为 Haaper 专有商业软件并排除第三方材料；Claudian 继续遵守 MIT。补充 Claude Agent SDK 商业条款、API Key/BYOK、禁止个人 OAuth 代转与品牌边界，新增 109 个生产包的完整许可证生成器和构建前审计；bundle banner 强制要求商业分发携带三份许可文件。许可证审计、Node 语法检查、类型检查、自测 62/62 和生产构建通过。正式售卖前仍需准备客户 EULA/订单条款，本次不替代律师审阅。
- 2026-07-01 TALOS 重复设置页融合与纠错：确认磁盘仅有一个 TALOS manifest；插件列表出现两个 TALOS，是主插件与其内嵌工作台各注册了一张同名设置页，并非独立 Claudian 应被停用。现 TALOS 只注册一个设置入口，原完整工作台设置并入其中的「屈原 · 高级」标签；左侧栏也只保留一个 TALOS 图标。独立 Claudian 2.0.11 已恢复启用、manifest 与 649 条会话原样保留；误复制进 TALOS 的 649 条会话已删除，TALOS 保留自身 4 条会话，设置恢复为合并前的 33 键结构与屈原人格 system prompt。类型检查、定向 lint、生产构建和自测 62/62 通过。
- 2026-07-01 屈原语音对话 · 加入唤醒词：持续监听默认显示“等待唤醒”，只有转写包含“屈原”才进入 voice runtime；单说“屈原”会回应“我在，你说”，同句可直接说“屈原，帮我……”。唤醒后开启 30 秒连续对话窗口，每次语音交互刷新计时，说“退下”或超时后休眠；未唤醒内容不进入 Agent，打断/暂停后状态正确回落。门控位于 ASR 之后，因此本地 Whisper 保持本机处理，千问档仍会先把音频送云端转写。类型检查、定向 lint、生产构建和自测 55/55 通过；产物已部署到 `.obsidian/plugins/talos/`，`data.json` 哈希保持不变。
- 2026-06-29 屈原语音对话 · 加 Edge 免费中文 TTS：`voiceio.ts` 的 `StreamTts` 新增 `edgetts` 引擎（纯加法，不动现有 system/aliyun/elevenlabs，不影响右侧栏行为）——微软 Edge 朗读，免费、无 key、中文音色自然（晓晓/云希/云扬/粤语等）。走在线 WebSocket，鉴权用 URL 的 `Sec-MS-GEC` token（BigInt 算 Windows 文件时间 5 分钟取整 + SHA-256，浏览器 `crypto.subtle` 实现，无需自定义请求头故可直连）；每句开一条 WS 收 mp3 帧 → blob 交 `playUrl`。设置加 `edgeTtsVoice`（默认 zh-CN-XiaoxiaoNeural）+ 引擎下拉新增「Edge 微软·中文免费」。typecheck 0、lint 0、esbuild 0、selftest 25/25、产物已同步。⚠️ 未实机验证：需确认 Obsidian 能连 `speech.platform.bing.com` 且微软 token 方案未变；不行则回退系统语音/千问。至此免费闭环齐：STT=本地 Whisper(离线)+千问(云)，TTS=Edge(免费中文)+系统语音(离线)+千问(云)。
- 2026-06-29 屈原语音对话 · P2 本地 Whisper（可切换、永久免费档）：把麦克风+VAD+打断抽成基类 `vad-mic.ts`（含 `encodeWavBase64` 工具），`cloud-asr.ts` 瘦身为子类（仅 DashScope 转写），新增 `local-asr.ts`（`LocalAsr` 子类）——**运行时**从 CDN（默认 `esm.sh/@huggingface/transformers@3`，可设置覆盖）动态加载 transformers.js（不进 bundle，主包仅 +6KB），首次转写建 Whisper pipeline（优先 WebGPU、失败回退 WASM），对 16k Float32 直接识别；模型默认 `Xenova/whisper-base`，中文传 `language:chinese`。设置加 `quyuanAsrEngine`(cloud|local，默认 cloud)/`quyuanLocalAsrModel`/`quyuanLocalAsrCdn`；顶栏「千问⇄本地」按钮切换并持久化、重建续听。typecheck 0、lint 0（dynamic import 加 no-unsanitized 豁免说明）、esbuild 0（动态 import 保留为运行时、未静态打包）、selftest 25/25、产物已同步。⚠️ 本地档**未实机验证**：需确认 Obsidian Electron 能 `import()` CDN ESM + WebGPU/WASM 可用 + 首次下模型；不行则留在千问云端档。
- 2026-06-29 屈原语音对话 · P5 放开任务执行：语音模式由「只对话不调工具」改为「闲聊直答、要做事时可调工具执行并口头回结果」。`voice-driver.ts` 改 `VOICE_MODE_PREFIX`（允许新建/搜索/打开/整理等工具，要求每轮口语结尾）；新增 `sawTool` 跟踪：若一轮只执行工具无口头结尾，优雅兜底回「好的，已处理。」并入历史+朗读，不再退化为报错。审批仍走 `evaluateQuyuanToolPolicy` 治理（deny 拦截）。typecheck 0、lint 0、selftest 25/25、产物已同步。⚠️ 待实测：① 工具执行类指令端到端（如"记到收件箱"）是否真落地+口头确认；② 破坏性操作的语音二次确认尚未做（当前由治理兜底），后续 P5 细化。
- 2026-06-29 屈原语音对话 · 止住 VAD 误触发洪流：实机显示持续监听把噪声/屈原朗读回声当说话，ASR 幻觉成「run」狂灌引擎 → 大量 ede_diagnostic（但曾成功回一次，证明链路通）。收紧 VAD：START_RMS .02→.03、START_FRAMES 3→4、MIN_SPEECH_MS 300→500、新增整段峰值门槛 MIN_PEAK_RMS .045（噪声直接丢弃不送识别）；新增 `setBusy()`——屈原思考/朗读时**暂停正常采集，仅保留高阈值持续打断**（BARGE_RMS .06 / ≈320ms），从根上掐断回声自激；面板 respond 起设 busy、done/error 解除，并加在途守卫防 barge-in 悬挂。typecheck 0、lint 0、selftest 25/25、产物已同步。⚠️ 阈值仍可能与具体麦克风不匹配：若变成"说话没反应"=门槛偏高需调低，若仍乱触发=需再调高；引擎对干净单句的 ede_diagnostic 残留率待实测。
- 2026-06-29 屈原语音对话 · 修复引擎不产出（审批回调缺失）：实机暴露 driver 反复「未就绪」。根因有二：① 误把 `ensureReady()` 当门槛——v2 运行时实际是 query() 时按需启动（Tab 注释明示），已删该门槛；② 缺审批回调——超级大脑系统提示令屈原启动即 `Read` 人格文件，首轮即工具调用，无应答方则悬停无文字输出。现 `voice-driver.ts` 在建 runtime 时 `setApprovalCallback`（复用 `plugin.evaluateQuyuanToolPolicy` 治理裁决，语音无弹窗故 'ask' 放行、'deny' 仍拦）+ `syncConversationState(null,[])` 被动初始化会话。typecheck 0、lint 0、selftest 25/25、产物已同步。⚠️ 待实机确认现在能出回复。
- 2026-06-29 屈原语音对话 · 持续监听 + 打断（免按键）：整条链路已实机跑通（识别→v2 引擎回复→朗读）。把云端识别从「按键推挽」升级为**持续监听 + 能量 VAD 自动断句**：`cloud-asr.ts` 改为常驻 AudioWorklet 取帧→主线程算 RMS→状态机(候命/开口含 pre-roll 防截头/句末静音 800ms 结束)→自动转写；getUserMedia 开 echoCancellation/noiseSuppression/autoGainControl 减回授。进屈原页**自动开麦**(无 key/无权限给提示)，底栏按钮改为「聆听中/已暂停」开关。新增**打断(barge-in)**：用户在屈原思考/朗读时开口 → `onSpeechStart` → `driver.cancel()` + `tts.stop()`。typecheck 0、lint 0、selftest 25/25、产物已同步。⚠️ VAD 阈值(START_RMS .02 / KEEP_RMS .012 / 静音 800ms)未实机调过，若误触发/不触发需按环境调；唤醒词「嘿屈原」尚未做(当前=全程持续监听)。
- 2026-06-29 屈原语音对话 · 接入云端 STT（WebSpeech 失败修复）：实机确认 WebSpeech 在 Obsidian Electron 报 `network` 不可用（起得来但收不到结果）。新增 `src/quyuan/cloud-asr.ts`（`CloudAsr`）：按一下录音(AudioWorklet 采 PCM)、再按一下编码 16k 单声道 WAV → base64 内联 → 经 `requestUrl`(bearer，复用 `settings.aliyunApiKey`，与 TTS 同鉴权) POST DashScope `qwen3-asr-flash` 同步接口(`multimodal-generation/generation`)，取 `output.choices[0].message.content[0].text`。`voice-panel.ts` 用 `CloudAsr` 替换 `MicStt`(推挽式、无中间结果)，识别文字 → `QuyuanVoiceDriver` → 流式回复+朗读。typecheck 0、lint 0(改用 AudioWorklet 避开 ScriptProcessor 废弃告警)、build 通过、selftest 25/25、产物已同步 `.obsidian/plugins/talos/`、bundle 含 `qwen3-asr-flash`。⚠️ 仍需实机验证整条链路(需 `aliyunApiKey` 已配 + 麦克风权限)：进屈原页 → 点「说话」录 → 再点停 → 看是否出识别文字 + 屈原流式回复并朗读；driver 的运行时装配(approval/会话)若报错据此再补。
- 2026-06-29 屈原语音对话升级 P0（UI 骨架，进行中）：新增独立面板 `src/quyuan/voice-panel.ts`（`QuyuanVoicePanel`），Siri 式五状态语音球 + 实时对话区 + 引擎切换，复用 `jarvis/voiceio` 的 `StreamTts`/`MicStt`，`.tq-` 作用域自注入样式隔离；右侧栏 `JarvisView`/`JarvisAgentPanel` 与 `jarvis-view.ts`/`panel.ts` **零改动**。`view.ts.pageJarvis` 主页内嵌「屈原」页已改挂此面板。typecheck 0 错、生产 build 通过、bundle 含 `tq-voice`、侧栏类 `jv-agent`/`talos-jarvis-view` 仍在。集成方向已定（Haaper 选「语音壳驱动 v2 引擎」）：新增 `src/quyuan/voice-driver.ts`（`QuyuanVoiceDriver`），经 `ProviderRegistry.createChatRuntime({plugin,providerId})` 取得与 v2 工作台同源的运行时，按官方契约跑 `prepareTurn → query` 流式循环，把 `StreamChunk`（text/tool_use/error/done）映射为语音壳回调；面板 `respond()` 接 driver：边收 text 增量边渲染气泡 + 喂 TTS 朗读，工具调用回显，done 时 flush。typecheck 0 错、生产 build 通过、bundle 含 `tq-voice` 与 `createChatRuntime`。
入口路由已接：主页「屈原」三处入口（Logo `openQuyuan` / 模块卡 / 命令条）统一改为 `openQuyuan()` → `activePage="jarvis"`，渲染控制台内的语音页（`pageJarvis` 挂 `QuyuanVoicePanel`），不再打开 v2 工作台 tab；v2 工作台由命令「打开屈原完整工作台」单独进入（语音壳已用其引擎）。`.tq-` 样式迁入 `styles.quyuan-shell.css` 走 build-styles 合入（不再注入 `<style>`）。typecheck 0、定向 lint 0、生产 build 通过、selftest 25/25（已更新第 102 行断言对齐新设计）、产物逐字节同步 `.obsidian/plugins/talos/`、`data.json` 未改。
⚠️ **唯一未完，诚实标注**：`QuyuanVoiceDriver` 仅过类型检查/lint/打包，**未经 Obsidian 实机验证**——v2 运行时很可能还需 `setApprovalCallback`/`syncConversationState`/会话装配才能真正流式产出，且需 Provider 凭证。须在真实 Obsidian 联调：进主页屈原页 → 麦克风说话 → 看是否流式回复+朗读；报错据此补运行时装配（对应 P1/P2/P4 联调）。
- 2026-06-29 控制台屈原入口修复：诊断确认点击事件与 `talos-quyuan-view` 均正常，失效感来自入口仍按 Claudian 设置打开右侧栏；右栏收起时主区域不切换，视觉上等同“无反应”。现将控制台内部的导航、动态 Logo 和命令条统一改为在 Obsidian 主区域新标签打开屈原 v2，并复用已存在的主区域屈原标签；侧栏 ribbon/命令入口继续保留原右栏行为。两条路径均增加打开失败 Notice。typecheck、生产 build、定向 lint 和自测 25/25 通过。
- 2026-06-29 屈原 v2 v1 视觉壳响应式优化：保留 v2 的权限、能力、图像、语音、停止、发送、模型、思考、上下文、MCP、YOLO 等全部入口；将固定像素与按全窗口计算的 `vw` 改为按屈原面板宽度计算的容器响应式尺寸。二次实机截图发现 440px 断点仍会把“能力”和“语音”移到第二排，并将“权限”缩成单字；现已删除换排逻辑，顶部固定三列单排、底部固定单排，320px 起仅缩小间距/字号/开关但不换位。语音开关改为真实 checkbox + track/knob 结构，解决缩放时圆点脱轨。788/504/405/390/320 五档均无横向溢出；交互回验与页面错误检查通过，自测增至 22/22，生产产物已同步 `.obsidian/plugins/talos/` 且 `data.json` 哈希未变。
- 2026-06-29 屈原 v2 工作台 UI 优化收尾（codex 续作）：codex 对嵌入式 Claudian 工作台 chat 层（`ClaudianView`/`Tab`/`TabBar`/`ConversationController`）、输入工具条（`InputToolbar`）与图片上下文（`ImageContext`）做 UI 优化后额度耗尽，停在「build→部署→验证」收尾步未完。本会话补完：`tsc --noEmit` 通过 → 生产 build（esbuild，沙箱补装 `@esbuild/linux-arm64` 匹配二进制）→ 屈原自测 **14/14** → 改动文件定向 lint **0 error** → `node --check main.js` 语法校验通过；`main.js`/`styles.css`/`manifest.json` 已同步 `.obsidian/plugins/talos/`（与构建产物逐字节 `diff` 一致），`data.json` 未改。⚠️ 诚实标注：codex 此轮 UI 优化的**具体改动意图**因无 git 基线/codex 转写不可考，待 Haaper 确认后补精确描述；产物有效性已验证，但 Obsidian 实机往返（多 Provider/标签/图片/输入工具条）仍需人工回验。
- 2026-06-29 v0.2.0 启动冲突修复：实机截图显示 `talos-console-view` 因插件未激活而失效。根因是内嵌工作台沿用上游 `claudian-view` 与已安装的独立 Claudian 重复注册；同时发现共享 `.claudian/` 会形成数据互扰。现将屈原 v2 视图隔离为 `talos-quyuan-view`，存储隔离到 `.talos/quyuan/`，不再读取 `.claude/` 或 `.claudian/` 旧路径。typecheck、自测 14/14、生产 build、定向 lint、命名空间扫描与 bundle 装载 smoke 均通过，修复产物已重新部署。
- 2026-06-29 屈原 v2 核心融合（#73-B）：TALOS 主插件继承并内嵌 Claudian 2.0.25 通用 Agent 工作台，新增 `src/quyuan/` 人格/治理/能力契约层；全文注入屈原人格上下文，缺失即 fail-closed；工具审批接 TALOS 治理，行内编辑先读目标 `_README.md`，流式回复接回现有 TTS。v1 `src/jarvis/` 与旧入口保留回滚。typecheck、自测 12/12、生产 build 与定向 lint 通过；待 Obsidian 实机往返与数据迁移验收。
- 2026-06-28 全页铺满与输出页防裁切：移除控制台外层 1500/1520/1540px 宽度上限，七主题统一占满 Obsidian 可用工作区；所有双栏网格改为 `minmax(0,1fr)`，面板与页容器允许正确收缩。输出作战室长标题和状态由单行省略改为完整换行，右侧运营候选不再被左栏挤出。1728×1084 回验中根容器与视口同宽、双栏等宽且 9 条内容零裁切；390×844 自动单列且无横向溢出。构建通过并同步插件目录。
- 2026-06-28 退役每日操作系统 HTML：删除根目录 `每日操作系统.html`；`每日操作系统.md` 改为 TALOS「每日执行舱」迁移说明，插件内原生页面、实时数据、命令和七主题不受影响。同步清理 HTML 索引、视觉来源说明和相关变更记录。
- 2026-06-28 集成每日操作系统：导航在“屈原”下新增“每日执行舱”，将旧版每日操作系统的零决策执行轨道原生化，不再通过 iframe 嵌入。页面实时解析 `tasks.md` 焦点、优先级和 `done_when`，按当天高亮周轮值；四个命令按钮支持复制调用，时间块、焦点、轮值日和执行入口可打开真实库文件。七套主题新增 `daily` 页面变量与组件材质，支持统一选中反馈和 1100/680px 响应式。构建、定向 lint、正则样例与浏览器首屏结构检查通过。
- 2026-06-28 屈原单页工作区：仅在 `jarvis` 页面隐藏常驻 TALOS Hero 与底部说明，保留左侧导航；右侧 `.page-content/.jv-panel` 改为占满视口可用高度，消息区弹性扩展并独立滚动，输入区固定在面板底部，输入框增高到 76px。桌面同参考截图尺寸回验中消息区约 951–979px，七套主题均无横向/纵向页面溢出；390px 窄屏回退为页面内滚动且无横向溢出。其他九个页面布局不变。
- 2026-06-27 全主题统一模块交互反馈：把能力中心命令卡已有的颜色反馈扩展到总览、输出、TALOS、收件箱、健康、项目、知识与全库页面的指标卡、项目卡、知识节点、列表行、闸门、信号条、分布条等原子模块。修正首轮只绑定点击选中的问题，现与导航栏一致：鼠标悬停或键盘聚焦立即变色，点击后仍保留当前高亮并按页面记忆选择；补齐 Enter/Space 与 `aria-pressed`。七套主题分别使用极光辉光、宇宙渐层、小岛按压、Mac 黑白反相、数据流荧光填充、柔光内凹、几何主色块。构建、`src/view.ts` 定向 lint、浏览器 CSS 解析与七主题样式检查通过。
- 2026-06-27 新增 Retro Design System 32/37/42 三主题：设置页新增 `数据流 · 动态终端`、`柔光浮雕 · Neumorphism`、`几何现代主义 · Bauhaus`，主题总数增至七套。32 使用最多 20 列、仅 transform 的低负载字符流（移动端减至 10 列），移除电影专有文案；37 将同材质双阴影、凹凸按压态映射到全部 TALOS 控件；42 将纸张、黑线框、红黄蓝几何构图映射到完整控制台。三套都为十个页面设置独立主题变量，覆盖导航、卡片、进度、热力图和屈原，并支持响应式与 reduced-motion。`npm run build` 与本地桌面/390px 视觉回验通过，详见 `design-qa.md`。
- 2026-06-27 默认主题统一内容操作按钮：仅 `Aurora 原版（默认）` 的导航、快捷入口、能力中心标签与“打开源文件”入口使用 Uiverse.io（rahulgarg99）黑白胶囊按钮，通过 Obsidian 内置 Lucide 箭头生成双图标滑入动画；据 Animal Island/Macintosh 实机截图二次修正为 DOM 级主题隔离——非 Aurora 元素不再挂 `.button1` 类，彻底恢复各自原按钮模块。Nebula、Animal Island、Macintosh 与屈原面板按钮不变。
- 2026-06-26 删除 Daylight 浅色宇宙稿主题：视觉风格由五套减为四套。`TalosVisualTheme` 移除 `cosmos-light`，`normalizeVisualTheme()` 去掉 `cosmos-light/daylight` 分支（旧存档值现回退默认 `aurora`），设置页下拉删去「Daylight 浅色宇宙稿」选项，`view.ts` 类名清理列表移除 `theme-daylight/theme-cosmos-light`；`styles.css` 将所有 `:is(.theme-cosmos-dark,.theme-cosmos-light)` 共享选择器收敛为仅 `.theme-cosmos-dark`，并删除全部 cosmos-light 独有规则块（深色宇宙 Nebula 主题不受影响）。`npm run build` 通过，产物已同步 `.obsidian/plugins/talos/`。
- 2026-06-26 新增 Macintosh 知识工作站主题：`TalosVisualTheme` 加 `system-classic`，设置页新增「Macintosh 知识工作站」，视图根节点支持 `theme-system-classic`；`styles.css` 新增完整 Classic Mac 主题覆盖，包括浅灰桌面背景、每页独立桌面纹理变量、条纹窗口标题栏、2px 黑边、方形按钮反相按下态、单色进度条/热力图、复古屈原面板和低成本动态桌面网格。参考仓库 `sakofchit/system.css`（MIT，System 6 单色 Mac 视觉），未复制外部字体/图标/源码。`npm run build` 通过，产物已同步 `.obsidian/plugins/talos/`。`npm run lint` 仍被既有 `src/jarvis/engine.ts` 类型规则报错拦住（非本次主题改动引入）。
- 2026-06-26 新增 Animal Island 小岛主题：`TalosVisualTheme` 加 `animal-island`，设置页新增「Animal Island 小岛主题」，视图根节点支持 `theme-animal-island`；`styles.css` 新增完整小岛主题覆盖，包括浅色羊皮纸背景、每页独立背景变量、轻量动态波点/风带纹理、圆润卡片、胶囊按钮、游戏按压反馈、暖色屈原面板和部署目录同步。参考仓库 `guokaigdg/animal-island-ui`，未复制外部源码/字体/图片。`npm run build` 通过，产物已同步 `.obsidian/plugins/talos/`。`npm run lint` 仍被既有 `src/jarvis/engine.ts` 类型规则报错拦住（非本次主题改动引入）。
- 2026-06-25 回滚全幅宇宙舞台尝试：撤销上一轮导致首页侧栏/下方模块消失的 `overview` 全幅覆盖（隐藏侧栏、`page-surface`、首页舞台尺寸/节点重排等），恢复此前版本布局；保留三主题设置与“详情页隐藏大宇宙中控、使用紧凑页头”的优化。`npm run build` 通过，产物已同步 `.obsidian/plugins/talos/`。
- 2026-06-25 优化宇宙中控作用域：`Nebula 深色宇宙稿` / `Daylight 浅色宇宙稿` 的完整 `cosmos-orbit` 六节点宇宙中控仅在总览页显示；其他页面隐藏大轨道节点和底部命令条，改为轻量玻璃页头，随当前页面显示不同标题/说明，并保留各页独立动态星云背景但降低背景星球/轨道干扰，避免遮挡正文信息。`npm run build` 通过，产物已同步 `.obsidian/plugins/talos/`。`npm run lint` 仍被既有 `src/jarvis/engine.ts` 类型规则报错拦住（非本次 UI 改动引入）。
- 2026-06-25 视觉风格改为三主题并修正设计稿还原：`Aurora 原版（默认）` 保持当前插件设计不动，新增今天设计稿方向的 `Nebula 深色宇宙稿` 与 `Daylight 浅色宇宙稿`。实现：`TalosSettings.visualTheme` 三值 + `normalizeVisualTheme()` 兼容旧 `daylight` 存档值 + 控制台根节点 `theme-cosmos-dark/theme-cosmos-light`。新主题新增独立 `cosmos-scene` 宇宙中控层（中央 TALOS 核心、六个轨道节点、星球/星轨背景、底部命令条），并给根节点挂 `data-talos-page`，为总览/屈原/输出/TALOS/收件箱/健康/项目/知识/能力/全库十页分别设置星云色、星球材质、轨道倾角、光晕与动态参数；星点漂移、星球呼吸、轨道场、轨道环、中央核心均有持续动画，不再只是 Aurora 换色。`npm run build` 通过，产物已同步 `.obsidian/plugins/talos/`。`npm run lint` 仍被既有 `src/jarvis/engine.ts` 类型规则报错拦住（非本次主题改动引入）。
- 2026-06-25 修语速对 API 引擎无效：`ttsRate` 原只套在 `SpeechSynthesisUtterance.rate`（系统引擎），aliyun/elevenlabs 走 `<audio>` 播放未应用。修：`playUrl` 设 `audio.playbackRate`（钳 0.25–4）+ `preservesPitch=true`（变速不变调）。每句实时读设置，改即下句生效。音高 `ttsPitch` 仍系统引擎专属（audio 元素无调音高接口）。
- 2026-06-25 修「只第一轮朗读、之后哑掉」（API 引擎）：`StreamTts.stop()` 用 `audio.pause()` 打断在播音频时不触发 `onended`，`playUrl` 的 await 永久挂起 → `playing` 卡死 true → 后续回合 `pumpAudio` 全被 `if(playing)return` 挡掉。修：播放加代际令牌 `gen`（stop 自增）、`playUrl` 加 `onpause→finish` 解除挂起、`stop()` 复位 `playing`、`pumpAudio` finally 仅同代际才复位。仅影响 aliyun/elevenlabs（system speechSynthesis 不走此路径）。
- 2026-06-25 修 B 方案加载即崩（插件"消失"）：打包后 claude-agent-sdk 顶层 `createRequire(import.meta.url)`，cjs 产物里 `import.meta.url`=undefined → 加载抛 `ERR_INVALID_ARG_VALUE`，Obsidian 隐藏插件。esbuild 加 `define: {'import.meta.url': 'file:///talos-plugin/main.js'}`（合法占位；运行时不真用它解析原生二进制，因始终传 pathToClaudeCodeExecutable）。node require 冒烟：默认导出=插件类 ✓。已重建部署。
- 2026-06-25 屈原 B 方案（agentic 重做，1:1 对标 Claudian + 语音）：新增 `src/jarvis/`（engine 流式 SDK 会话 + voiceio 流式 TTS/WebSpeech STT + panel 流式文本/工具卡/权限审批 UI/麦克风），deps 加 `@anthropic-ai/claude-agent-sdk`，esbuild external 补 `node:` 前缀内建，settings 加 5 项（jarvisClaudeBin/Model/PermissionMode/SttEngine/SttLang），styles 加 `.jv-agent/.jv-tool/.jv-perm`。view.pageJarvis 接线换到新面板；**旧 `voice.ts` 不动、保留存档**。bundle 851K。tsc+build 通过，已同步 `.obsidian/plugins/talos/`。⏳ 待用户在 Obsidian 实测首轮流式往返（运行时依赖本机 claude 可执行 + ANTHROPIC_* 环境）。
- 2026-06-25 修旧屈原切页后无法输入（在 B 方案前）：`voice.ts` unmount 硬复位 busy/中止子进程 + gen 代际守卫。（旧文件，现已停用但保留。）
- 2026-06-24 思考中卡死·三修（根因=GUI 缺 shell 环境）：终端 claude 能答、插件 spawn 的 claude 卡死——因 Dock 启动的 Obsidian 拿不到 `~/.zshrc` 的 `ANTHROPIC_BASE_URL/AUTH_TOKEN`（智谱 GLM 网关配置），spawn 的 claude 连不上模型。新增 `getShellEnv()`：用 `$SHELL -lic env` 捞一次登录 shell 完整环境（缓存），spawn claude 时带上。另查明用户 claude=智谱 glm-5.2、三个智谱 MCP（zread/web-search-prime/web-reader）连不上已移除。超时放宽 120→300s（GLM xhigh effort 慢，"你好"churn 58s）。build+lint+load 通过，已同步。
- 2026-06-24 思考中卡死·二修：「你好」也超时 → 不是工具权限（无需工具），疑注入的 `--allowedTools/--disallowedTools` 与库自带 `.claude/settings.json`（含 MCP 搜索 + 安全 bash 白名单）冲突。改 `voicePermission` 默认 `off`（不注入标志，交库配置管），dropdown 把「不加」置顶为推荐；prompt 回到参数传 + `stdin.end()` 关闭防空等。data.json 同步改 off。build+lint+load 通过，已同步。
- 2026-06-24 修「一直思考中」卡死：加权限标志后 `claude -p` 的 prompt 可能没被 `-p` 接走，导致 `-p` 退而空等 stdin → 永久挂起。改为 prompt 走 stdin（`child.stdin.write`+`end`，headless 标准用法），args 只放标志；超时 180→120s + 提示可切「工具权限=不加」。build+lint+load 通过，已同步。
- 2026-06-24 接阿里云千问 TTS：新增第三语音引擎 `aliyun`（中文最佳、有免费额度）。`aliyunTtsUrl` 走 DashScope multimodal-generation HTTP（Bearer），取 `output.audio.url` 用 Audio 播放；默认音色 Andre（磁性沉稳男声·屈原中文管家感）。settings 加 key/voice/model + 引擎下拉新增项。build+lint+load 通过，已同步。
- 2026-06-24 接 ElevenLabs 神经语音：设置加「语音引擎」(system/elevenlabs) + key/voiceId/model；`elevenLabsTts` 走 requestUrl 取 mp3 用 Audio 播放，speak() 分支、失败回退提示+气泡重念，elevenlabs 跳过静音保活。默认英音男声 Daniel（屈原气质，合成音色非真人克隆）。build+lint+load 通过，已同步。
- 2026-06-24 权限二修：`--allowedTools` 改逗号单值（`READ_TOOLS.join(",")`），兼容单值/变长两种解析，避免只有首个工具生效。注：库 `.claude/settings.json` 的 allow 用 MCP 搜索（web-search-prime），无内置 `WebSearch`，故需在该文件 allow 里手加 `"WebSearch"`,`"WebFetch"`（cowork 侧该文件受保护改不了，需用户手动）。build+lint 通过，已同步。
- 2026-06-24 工具权限设置：无头 `claude -p` 无法弹授权框，导致 WebSearch 等被卡。新增「工具权限」下拉（readonly 默认/acceptEdits/all/off），`permissionArgs()` 把标志拼进 argv（绕开引号切分），默认只读+搜索（可读库、可联网搜，禁写/禁 bash）。命令保持 `claude -p`。build+lint+load 通过，已同步。
- 2026-06-24 修朗读中断+页面闪跳：根因——`claude -p` 在库内读写 `.md` 触发插件 vault.modify 监听 → `refresh()` 重绘控制台 → `renderPage` 卸载屈原（`unmount`→`cancelSpeech` 打断朗读）并重建（闪跳）。修：view 加 `jarvisMounted`，`refresh()` 在屈原页活跃时早退（不重绘），`renderPage` 对已挂载的屈原页保持不动、仅换页时卸载。朗读与对话状态在后台刷新中不再被打断。build+lint+load 通过，已同步。
- 2026-06-24 修自动不朗读（根因定位）：现象「点重念能读、生成后自动不读」= Chromium 自动播放策略——长生成后用户手势已过期，程序 `speak()` 被静默拦截。解法静音保活：`startWarmup` 在发送手势内持续播放 volume0 占位句（onend 自循环）撑住音频会话，回答到达后 `warming=false` 停补充、`speak(answer, continued=true)` 把真内容无缝接在队列后（引擎不曾停 → 不被拦截）；错误/停止走 `cancelSpeech`。build+lint+load 通过，已同步。
- 2026-06-24 修长回答不朗读：测试句能念、claude 长回答不念——根因 Chromium「过长 utterance 不发声 + 15s 自动暂停」。speak() 改为 `chunkText` 按句切块（句末标点/120 字）逐条排队朗读，加 `keepAlive`(8s `resume()`) 防截断；回答气泡加「点击重念」(hover 显🔊)作 autoplay 兜底。build+lint+load 通过，已同步。
- 2026-06-24 界面优化：① 全控制台统一字体——新增 `--sans`(PingFang SC/系统)挂在 `.talos-console` 根，正文不再随 Obsidian 主题漂移；`--mono` 仍管数据/标签，形成「正文 sans + 数据 mono」双字体体系。② 2D 形象重画——天线脉冲、侧传感器、圆角头+面罩、额心、成组眨眼的眼球+高光、静态微笑↔说话张合、思考点；状态色（待命青/思考琥珀/说话绿/错误玫红）。③ 屈原页排版——状态条带状态圆点(data-state)，提示卡片化。build+lint+load 通过，已同步。
- 2026-06-24 朗读加固：speak() 修 Electron/Chromium 两个老 bug——持 utterance 引用防 GC、cancel→speak 错开一帧避免被丢、入队后 `resume()`、嗓音异步未就绪时听 voiceschanged 再念（单次启动守卫防重念），onerror 把错误码弹 Notice。对话区加「🔊 测试声音」按钮（点击直接念固定句，隔离 TTS 可用性）。build+lint+load 通过，已同步。
- 2026-06-24 声音可调：「TTS 嗓音」文本框升级为下拉（列系统实际可用语音，含自动 + voiceschanged 异步补全），新增「语速 ttsRate」「音调 ttsPitch」滑块，speak() 套用。build+lint+load 校验通过，产物已同步。
- 2026-06-24 屈原一期：导航新增「屈原」语音助手页。新增 `src/voice.ts`（SVG 角色 + `claude -p` 大脑 spawn + speechSynthesis 朗读 + 口型/表情联动 + 对话 UI），settings 增 5 项（voiceAgentCommand/voicePersona/voiceLang/ttsVoice/live2dModelPath），view 接入 PAGES + pageJarvis（跨切页保留会话、切走停朗读），styles 增 `.jv-*` 主题与动效。预留 Live2D 接入槽（一期 b）与 STT 占位（二期）。build+lint 通过，产物已同步 `.obsidian/plugins/talos/`。
- 2026-06-23 v0.1.0：全库扫描 + 实时统计 + 13 模块地图 + 发布作战室 + 能力中心。已装 `.obsidian/plugins/talos/` 并启用。
- 2026-06-23 改版：外观复刻 dashboard.html 极光玻璃态；新增侧栏（时钟/快捷入口/健康snapshot/待审批）；新增**能力中心**——读库内真实 12 命令 + health-scanner agent + 3 个 source-command agent-skills + 工作流，三标签切换、点击复制调用。build/lint 通过、bundle 加载 + 能力扫描校验通过。
- 2026-06-23 多页改版：从单页长滚动改为多页路由（主页 + 切页按钮，每模块独立成页）；背景调至近黑深色。build/lint/smoke 通过。
- 2026-06-23 像素级复刻：styles.css 改为逐条移植 dashboard.html 的 `<style>`（scope 到 .talos-console，沿用 card/panel/hero/nav/command/focus/stat/barchart/note 同名类），视觉与 HTML 一致。布局采用「侧栏当导航 + 主区切页」：左侧栏（时钟周历 / 导航 / 快捷入口 / 上下文健康 / 待审批）固定，点导航切右主区；Hero（含 TALOS logo SVG）常驻。背景按要求压暗（--bg #05060d）。
- 2026-06-23 分页功能化第一轮：保留当前「模块 + 分页」信息架构，不回退旧 HTML 单页布局。总览页新增运行快照与信号入口；系统模块页补 README 存在性、最近更新文件与双层跳转；能力中心支持复制调用 + 打开源文件；今日焦点、待审批、偏好候选、发布闸门均可点击回到真实源文件。build/lint 通过，产物已同步 `.obsidian/plugins/talos/`。
- 2026-06-23 模块重构：左侧导航改为高频入口（总览 / 输出作战室 / TALOS 漏斗 / 收件箱消化 / 系统健康 / 项目场景 / 知识枢纽 / 能力中心 / 全库视图）；新增 `src/data/navigation.ts` 结构化读取 `输出/`、`00-收件箱/`、`System/working-memory/`、`04-项目/`、`02-洞察/`、`03-素材/` 与 TALOS 七分区。总览页变为 8 个高频动作入口，旧系统模块/分布/热力图合入“全库视图”。build/lint 通过，产物已同步 `.obsidian/plugins/talos/`。
- 2026-06-23 深色背景微调：进一步压暗 Obsidian 视图容器、插件根背景和玻璃卡透明度，降低极光层亮度，避免浅色主题底色透出。build/lint 通过，产物已同步 `.obsidian/plugins/talos/`。
- 2026-06-23 深色字体适配：提亮主文字、次级文字与弱提示色，倒计时等浅色文字同步提高透明度，适配更深的黑蓝背景。build/lint 通过，产物已同步 `.obsidian/plugins/talos/`。
- 2026-06-23 Hero 主张卡字体修复：给 quote 模块强制统一主文字/次级文字色与 `-webkit-text-fill-color`，避免深色背景下被主题规则压成暗色。build/lint 通过，产物已同步 `.obsidian/plugins/talos/`。
- 2026-06-23 Logo 模块化动效：Hero 顶部改为左文案 + 右侧独立 TALOS logo 模块；logo 增强为心跳缩放、三层脉冲外圈、渐变描边、核心变色与光晕跳动，并补移动端尺寸适配。build/lint 通过，产物已同步 `.obsidian/plugins/talos/`。
- 2026-06-23 首页启动：新增设置项「启动时作为首页打开」，默认开启。Obsidian 布局恢复完成后自动打开 TALOS 控制台；若已有 TALOS 视图则直接聚焦，避免重复创建。build/lint 通过，产物已同步 `.obsidian/plugins/talos/`。
- 2026-06-23 侧栏字体彩色统一：导航与快捷入口两组侧栏模块新增独立色谱，标题/右上角小字/条目文字统一为渐变彩色字体，快捷入口补 `action-card` 类避免影响其他命令卡。build/lint 通过，产物已同步 `.obsidian/plugins/talos/`。
- 2026-06-23 Logo 联动 Claudian：右侧动态 TALOS logo 改为可点击/键盘触发按钮，默认执行 `claudian:open-view` 打开 Claudian chat view；设置页新增「Logo 点击命令」用于调整命令 ID，并补 hover/focus 反馈。build/lint 通过，产物已同步 `.obsidian/plugins/talos/`。
- 下一步：Obsidian 内实测；「冻结天数」按 `freezeStartDate`（默认 06-19）算重估期天数，设置可调；能力项点击目前是复制调用文本，后续可接「直接触发 agent」；验证稳后退役 python+iframe 旧方案。
